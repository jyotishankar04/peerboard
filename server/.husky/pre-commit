#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Checking code quality before commit..."

# Run linting on staged TypeScript files
bun run lint

if [ $? -ne 0 ]; then
  echo "❌ Linting failed! Please fix the errors before committing."
  echo "💡 You can try running 'bun run lint:fix' to automatically fix some issues."
  exit 1
fi

echo "✅ Code quality check passed!"